name: geostories
description: GeoStories Application
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Authentication
  google_sign_in: ^5.4.4
  firebase_core: ^2.6.0
  firebase_auth: ^4.2.7

  # Management
  flutter_bloc: ^8.1.2
  provider: ^6.0.5

  # System
  shared_preferences: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  generate: true
